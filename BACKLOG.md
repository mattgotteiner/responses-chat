* Debug view where you can see the actual JSON log of all the events that are coming in
* ability to save JSON events in JSONL format as the come in wire over API so we can create proper e2e tests using snapshot
* Stop button to stop streaming
* token based auth / login
* copy button to get the text out of the box
* Token usage 
* Don't lock view on scrolling